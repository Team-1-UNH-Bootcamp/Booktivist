// onload ->
// Code to extract category name from URL
// const getUrlVars = ()=>{
//     const categoryName = [], hash;
//     const hashes = window.location.href.slice(window.location.href(indexOf('?')+1)).split('&');

//     for(i=0; i <hashes.length; i++){
//         hash = hashes[i].split('=');
//         vars.push(hash[0]);
//         vars[hash[0]] = hash[1]
//     }
//     return categoryName;
// }
// $.ajax(`/api/categories/${categoryName}`, {
// type: 'GET',
// }).then((response)=>{
// for each element of response: create card
// in card, loop through each response[i] for data
// append all to dom
// })

// each card gets preset id
// this id will reference a function on script.js, which will launch the modal
// and propogate information from table
