// load this script first on myLibrary and addbook
// will return if user is logged in or not
// then load other scripts

// Check if user is logged in by seeing if id
// const userId = ''
// if (userId === ''){
//    send to loginpage
// if userId is not null see if status is logged in
// } else{
// $.ajax(`/user/${userId}`{
// if (response === true) - log in
// else {send to login page}
// })
// }

// $('#loginButton').click(() => {
//   const userData = [
//     {
//       username: $('#userNameInput').val(),
//       password: $('#passwordInput').val(),
//     },
//   ];
// $.ajax('/api/user_data', {
//  type: 'GET',
//  data: 'userData'
// }).then((response)=>{
//  if response === ""{didn't work}
// else {logged in and go to home page, logged in status set to true}
// })
// });
